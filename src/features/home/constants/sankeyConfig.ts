import { chartPalette } from '@/shared/theme';

export const SANKEY_NODE_WIDTH = 15;
export const SANKEY_NODE_PADDING = 20;
export const SANKEY_HEIGHT = 500;
export const SANKEY_LINK_CURVATURE = 0.5;

export const SANKEY_COLORS = [...chartPalette, 'var(--color-brand)', 'var(--color-warning)'];
